# Audio Uploads

This directory stores voice note audio files uploaded by users.

Files are named using the format: `voice_{user_id}_{timestamp}_{unique_id}.{format}`

**Security Notes:**
- Access to audio files should be restricted to the owning user
- Files are stored in webm/ogg/mp4 format depending on browser support
